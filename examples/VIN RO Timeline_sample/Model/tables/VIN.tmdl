table VIN

	column VIN
		dataType: string
		summarizeBy: none
		sourceColumn: VIN

		annotation SummarizationSetBy = Automatic

	partition VIN = m
		mode: import
		source =
				let
				    Source = Table.FromRows(Json.Document(Binary.Decompress(Binary.FromText("i45W8vIzdIwycfUwcPM1MTGyMDNWio0FAA==", BinaryEncoding.Base64), Compression.Deflate)), let _t = ((type nullable text) meta [Serialized.Text = true]) in type table [VIN = _t]),
				    #"Changed Type" = Table.TransformColumnTypes(Source,{{"VIN", type text}})
				in
				    #"Changed Type"

	annotation PBI_ResultType = Table

